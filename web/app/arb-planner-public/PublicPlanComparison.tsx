/**
 * @deprecated Replaced by the shared, generic `@/components/arb-planner/PlanComparison`.
 * This re-export is kept only to avoid breaking any stray imports; new code
 * should import the shared component directly. Slated for removal.
 */
export { default } from "@/components/arb-planner/PlanComparison";
