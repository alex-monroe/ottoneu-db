/**
 * @deprecated Moved to the shared, generic `@/components/arb-planner/TeamRosterSection`.
 * This re-export is kept only to avoid breaking any stray imports; new code
 * should import the shared component directly. Slated for removal.
 */
export { default } from "@/components/arb-planner/TeamRosterSection";
